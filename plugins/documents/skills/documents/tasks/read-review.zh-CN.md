# 读取与审阅工作流

1. 运行 `scripts/inspect_docx.py`，先读取警告，再得出结论。
2. 确认是否存在批注、域和修订后，再用 `python-docx` 提取文字和表格。
3. 保留上下文：标题、表头、行标签、脚注标记、修订状态以及附近的限定条件。
4. 答案依赖位置、视觉分组、图表、形状或页面版式时，需要渲染。
5. 只读问题不应修改或重新导出文档。

检查器统计 OOXML 结构，但不解释绘图层文字、公式、嵌入文件和所有 Word 域。当这些限制影响答案时，明确说明。
