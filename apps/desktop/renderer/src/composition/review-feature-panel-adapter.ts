/** Keeps the Review panel behind the Workspace lazy-loading boundary. */
export {
  DesktopReviewPanel as ReviewFeaturePanel,
  GitChangesPanel as GitChangesFeaturePanel,
} from '@setsuna-desktop/feature-review/renderer/panel';
